# Web (Next.js)

```bash
npm install
npm run dev
```

Production bundle for Lambda: `npm run build:lambda` (outputs `.open-next/`). Configure `NEXT_PUBLIC_API_URL` for the API host.

See the repository [README.md](../../README.md) for local stack and deployment.
